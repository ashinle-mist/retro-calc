pages:
  stage: deploy
  script:
    - echo "Pipeline is working!"
    - ls -la
    - mkdir public
    - echo "<h1>Hello World</h1>" > public/index.html
  artifacts:
    paths:
      - public
  only:
    - main